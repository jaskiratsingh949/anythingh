<div class="container mt-4">
    <div class="d-flex justify-content-between">
        <h2>View Tickets</h2>
        <div>
            <a href="{{ route('tickets.create') }}" class="btn btn-primary">Create Ticket</a>
            <a href="{{ route('tickets.view') }}" class="btn btn-secondary">View Tickets</a>
        </div>
    </div>
</div>
